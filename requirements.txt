# Core
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.10.0
pydantic-settings>=2.6.0

# Claude Agent SDK
# Source: https://pypi.org/project/claude-agent-sdk/
# CLI bundled - separate installation not required
claude-agent-sdk==0.1.18

# SDK Dependencies (auto-installed, but explicit)
# Source: PyPI Dependencies
anyio>=4.0.0
mcp>=0.1.0
typing-extensions>=4.0.0

# In-memory cache with TTL
cachetools>=5.5.0

# SSE Streaming
sse-starlette>=2.1.0

# Logging
structlog>=24.4.0

# HTTP Client
httpx>=0.27.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0
pytest-cov>=4.0.0
